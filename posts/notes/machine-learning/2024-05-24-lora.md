---
date: '2024-05-24'
title: 'LoRA'
---
freezes the pre-trained model weights and injects trainable rank decomposition matrices into each layer of the Transformer architecture, greatly reducing the number of trainable parameters for downstream tasks.

...the learned over-parametrized models in fact reside on a low intrinsic dimension.

Existing methods either add adaptgers or optimize input layer activation.

prefix tuning has unpredictable performance. 

LoRA only adapts the attention weights and not the MLP modules. 
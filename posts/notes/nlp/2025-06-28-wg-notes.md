---
date: '2025-06-28'
title: 'Wg. notes'
---





TODO

"Outreach"

Previous strategies

- Going onto various discord servers and talking about word.golf.
- Advantages: high immediate users.
- Disadvantages; can get banned and generally treated disrespectfully


- In person posters, 100 laminated posters: on Brown Campus, and in Seattle various regions. This is probably one of the reasons we had ~50 daily visitors for 6mo period.


We want to persist the current prompt html so that it is restored on reload. When do we want to save? Before each postWord. When do we want to load? After postWord.

cut the words based on prompt.target and take the last. 
Display the last word.

speedometer,waypoint,map,speck,horizon,soundproof,hallway,
braid,tutu

idea: request animation frame for prompts. restore on reload.

why is prompt-word empty here?

sign in with google on tutorial, does not sign in --.
After first prompt completion, shows 29 jumps.
After lo


gout on finish screen and refresh, will not display new screen.

On first page load with non-google logged in account, it does not show the prompts. Once you start playing, it becomes clear that some state has persisted from the other stage. This is probably okay.

If a user plays for a certain number of propmts, and then decides to login to an existing non-google account, after one more jump we get reverted to a mismatched state. This is probably the state for the user at the time. 

RenderPrompts update: from previousWords / jumpsA / prompts.

[10] prompt[0][0] and prompt[0][1]
we can calculate distances for each of those words. s.t. we can place them correctly in the array.
[3] Buzzer [4] [0] [0] [1] [1]

prompts state at any time:
12 spots per prompt.
1 spots is filled with the start prompt-word. The last spot on the same prompt is filled with the target.
Across the prompts before words, they can be empty or tally.
After the prompt with the words, it is all empty.
To perform an update, we 1. turn the start prompt-words to a tally and 2. add the new prompt word at the specified spot by score.

The array is 5 prompt-box divs with 6 word-tally-container children each. We need scores from the prior runs. 

function renderPrompts(previous_words, scoresArray, prompts, jumpsA, score=null){
    array = [[[]*6]*5]
    #render done_prompts
    done_prompts = [prompts which appear in previous words]
    for i,done_prompt in enumerate(done_prompts):
	    set array[i][0] to done_prompt[0], class to prompt-word.
         for middle_word in done_prompt[1:-1]:
			set array[i][sim_idx(middle_word)] innerText to '', class to tally.
	    set array[i][-1] innerText to done_prompt[-1], class to prompt-word.
    
	prompt_idx = len(done_prompts) + 1
    #render current_prompt
    current_prompt = prompts[prompt_idx]
    set last_word to previous_words[-1], class to prompt-word.
    progress = [slice of previous_words after the last existing prompt]
    for word in progress[:-1]:
        set array[prompt_idx][sim_idx(word)] innerText to '', class to tally.
    array[prompt_idx][sim_idx(last_word)] = last_word
    set array[prompt_idx][-1] to current_prompt[1], class to prompt-word.
}


const LOCAL_STORAGE_VERSION = 'v2'; // increment this when you deploy updates
const storedVersion = localStorage.getItem('app_version');
if (storedVersion !== LOCAL_STORAGE_VERSION) {
    console.log('[word.golf] Clearing stale localStorage due to version change');
    localStorage.clear();  // or selectively clear known keys
    localStorage.setItem('app_version', LOCAL_VERSION);
}

  File "/Users/eriq/.pyenv/versions/3.11.0/lib/python3.11/site-packages/flask/app.py", line 1249, in make_response
    raise TypeError(
TypeError: The view function did not return a valid response. The return type must be a string, dict, list, tuple with headers or status, Response instance, or WSGI callable, but it was a User.


Gradient: 
- identified suspicious results where 5 different experimental conditions produced identical 0.1135 accuracy scores
•recommended testing the learned matrix on the same test set used for derivation to validate the approximation method
Suggested testing if averaged matrices point in similar directions rather than orthogonal directions
Recommended geometric analysis using PCA for dimension reduction to explore vector relationships
Eric noted that matrices should be similar if they truly indicate MNIST classification
Suggested testing early-to-late layer connections (second-to-last, third-to-last) following LRE paper methodology



calculate new Prompts -> currentPrompts

Espresso by Sabrina Carpenter? Sign me up

We need to handle logout better. When a logged in user completes a game and then logs out, the state changes to something else, that I believe is whatever the state was at log-in prior.

on login. we want to clear the prompts on the screen and then recreate them from data. 

Strategies to consider
- [TODO] Language Log. Draft 
- Posters
- Dirt.fyi. Literary people who have free time.
- Media outlets
- Attempting to get distribution through existing informal (one to two person blogs with outreach), such as dirt.fyi. This might involve interviewing etc. Literary 

- Blogging and increasing SEO.
- Twitch: asking small streamers to promote. Would require more long-term. Just Chatting.


Analytics
- Organic (Search, Google, ChatGPT)
- Social Media (Instagram, Tiktok)
- Referrals: through longform blog channel type stuff
- Word.golf: targeted instagram ads. Design better visuals.




What do I want to do with my life? I want to enjoy it to the fullest. This means not going into academia like my parents did. This means applying to interviews early and trying hard at them.

Todo:
- change prompts to display first and last with tallies in between.
- change tallies to stack.
- feedback on two jumps closer.
- shift focus to difficulty.
- experiment with removing a word after clicking in.

showBanner("Perfect!", "banner-perfect");
showBanner("Impressive!", "banner-impressive");
showBanner("Great", "banner-great");
showBanner("Good", "banner-good");
showBanner("Close call..", "banner-closecall");

Hi Heidi,
I'm making www.word.golf, a language sport with word vectors. I would love to hear more about your path and learn from your perspective!


we are writing renderPRomtps twice.
we need to clear jumpsA at transition.

Problem now is that the word-tally-container restricts the total width of the container.

jumpsA is not updated correctly.

You need to start promoting aggressively.

Broader appplication: less focused research.


if existing tally at position:
create a new div 
min-height: 100%, gap: 0.25rem with two tallys
else if existing tally-container at position:
add a tally to tally container.



Guardrail
- if the word places the user in a lower bucket, clicking the word does not do anything.

on the backend:
sim_intervals = [0.2,0.27,0.35,0.42]
for a score between 0 to 1

- resp not working.
- perfect
- patch 12 no 6thh child.
- showscreeen on finish
- screen should be disabled.


- problem: the div store is not the correct word.



🐏 Animal #695 🦆
I figured it out in 11 guesses!
🟥🟨🟨🟨🟨🟩🟩🟩🟩🟩🟩
🔥 1 | Avg. Guesses: 11
    
https://metazooa.com
#metazooa




- No changes to tutorial at moment, not super effective either way.
- We should think about changing "mercury to razor" heading.
- Move login off page to help, change help button. [E]

Let's say we have a prompts container with prompt-box children, prompt children of prompt-box, and prompt-word children of prompt. How do we calculate if a prompt-word has absolute position within e.g. 0.5rem of another prompt-word, and if so it should replace it?

- Start storing all user data regardless of login [J]
- Display streak for logged in users either way.

freeze the positions of .prompts .prompt-box .prompt and continue displaying them absolute, while appending current tag on top.

set pointer events of flex children to none except over children named 'prompt-word'
An indication of how well someone does.

Okay, is there maybe a debug flag in flask which will run the debug on local and serve otherwise?

[Internal] Must now set DEV=true python app.py to test. Added environmental variable to manage dev and prod.

renderLogin({email: 'eriq'})

squid-app-sn9bl.ondigitalocean.app.

what simple mechanisms can I use?

Design choices....
JSON.stringify words.

Maybe just a good score tracker.

change score tracker to do 6+
share screen: share a screenshot.

calculate difficulty metrics!
Help me write a difficulty metric calculation for word.golf. 
This should calculate how many routes lead to the jump.

Powerset: 21^3 ways. 

There is at least 1 way to reach the jump in 2, but are there more than 1 way? Also, are there more than 10 way to reach in 3?

gradient operation: what does it do. 

- defocus all other words on hover
- 3 x 3 grid. if someone doesn't get the right word, punish them immediately and skip to the next word.
- There are potentially 20 * 19 mental comparisons which must be made to understand which word is the closest to the final one.
- By reducing word count, we can make it 9 * 8 comparisons instead.
- Or what if it was just 4 words?
- We can make it a swipe game.


[Todo] Push login with main.
[Todo] Eric will pull login 
[Todo] Eric will push help screen branch with updated XML logic to main.

Eric 
- Develop help screens for intro sequence.
- Troubleshoot any remaining bugs in sequence
- [Todo] UI for login and sign up.

Julian
- [Todo] Login functionality


At the moment, we need to update the UI. 
We can do this by putting into a flex container.

Not possible to create a staging environment at the moment, unless we use two domains.


Eric
- Decided not to switch to sentence embeddings because they are worse.
- Found major fun improvements with 1. backoff and 2. selecting vocabulary.
- Developed a reproducible data pipeline for filtered embeddings + neighbors from word frequencies and word vector files.
- Deployed an update with a minimal complete screen and UI improvements.

Julian
- Setup DigitalOcean database for cookies and tracking users.
- Updated localStorage logic for ensuring saved state between reloads.
- Developed separate endpoint for sending database queries

Currently working on
- [Eric] Improving UI, adding lightweight animations to make more compulsively playable. 
- [Julian] Developing unobtrusive account registration process for users on first visit. When users create an account, it allows tracking over multiple devices. Otherwise, we use localStorage.
- Deciding how to structure help screen.

We decided that at a minimum, we would want to have account registration set up when we circulate the app to social networks. Let us know if this all sounds reasonable and if you have any comments. We don't feel a pressing need to meet at the moment, but maybe next Tuesday would work. 

Best,
Eric



Todo:

- Step 1: train the 3000 checkpoint on formal languages.

I have a trained model in output/wikitext/pythia-160m/checkpoint-3000. Please help me run the model on 


cd /oscar/scratch/exia3/




word.golf 

⬛⬛⬛⬛⬛
⬛⬛⬛⬛⬛
⬛⬛⬛🟨⬛
⬛⬛🟨🟨⬛
🟩🟩🟨🟨🟩
🟩🟩🟨🟨🟩

Please identify the words from this list which would be recognizable to an American with a U.S. High School education. Please only output those words, don't add any other text.

nil,decider,measly,lose,paltry,decl,loss,match,bossed,equal,goal,zero,furst,slim,defeat,minus,solitary,canter,marginally,whilst,margin,concede,theyr,mbo,hoodoo,lati,miniscule,diluted,tonne,naught,whistle,lowly,unlucky,satis,econd,flat,score,footy,draw,cup,final,thrashing,rue,bucketload,onesided,trounce,priceless,lopsided,plus,residual,bereft,nonexistent,shitload,gst,point,shambolic,bossing,reckon,pitiful,win,libor,almost,ascendency,howler,clinch,crushing,aunties,penetrative,mre,decent,share,gasp,garra,adrift,weere,nigh,halfpenny,finis,facile,nada,annihilation,undefeated,stalemate,pensionary,milliard,trumps,unanswered,hase,saria,thumping,min,comfortably,fluffing,regas,rugby,evened,patchy,unf,unreconciled,boosted,pissant

I want to run the '/' page with the prompt [outside, layer], and then [mercury, razor]. In order to do this,

Implicit multimodal alignment.: 

@channel We will go over this in the meeting but we also would like you all to meet on your own time at least once a week, and have an internal chat for getting to know your teammates. What's worked in the past is a Discord group DM, so please reply to this message with your ID (or let me know if you don't have one and we can try something else).

Internal meeting. Brainstorm. Action Items will be in the Slack. 

outside --> beneath --> surface --> layer
mercury --> toothpaste --> razor


breakfast --> menu --> palette
mystery --> detective --> medic

I'm getting a new data['prompts'], so I think that data['i'] can be incremented, but this isn't true.

renderPrompts: promptTexts=shrug,halter,studio,blade,outside,layer,contraction,wraith,assortment,intention, i=2, jumpsA=2,2, current_jumps=0


Please take a look at this list and return a version which is filtered for the first and last words being interesting nouns, with a bias towards both being interesting and concrete words. Words like 'peril' and 'predicament' are okay. 

You can be stricter. I don't want examples like "troop,deploy,launch" or "atonement,sacrifice,hardworking", and "restraint,proclivity,intent". Avoid two or more vague words.


You are still not filtering any words. Please think step by step. Remove at least 30% of them for vagueness, and tell me the criteria you use.


At the moment: try to replace the main words with an updated version.
